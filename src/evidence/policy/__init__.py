"""Reading the Policy: coverage verdicts, quote validation, and text slicing.

Shared by `src.evidence.fraud_detection`, `src.strategies.fast_path`, and Strategy 2's
own Channel C (`src.strategies.strategy2.model`) -- none of it prices anything."""
