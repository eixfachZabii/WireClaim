[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "wireclaim"
version = "0.1.0"
description = "Scheduled ingestion and processing scaffold for EHL Claim to Fame cases"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["src"]
